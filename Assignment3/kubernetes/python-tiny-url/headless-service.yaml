apiVersion: v1
kind: Service
metadata:
  name: python-tiny-url-service
  namespace: backend
  labels:
    name: python-tiny-url-service
spec:
  clusterIP: None
  ports:
    - name: python-tiny-urlport
      port: 5000
      targetPort: 5000
  selector:
    app: python-tiny-url
